<!DOCTYPE html>
<html>
  <head><title>Users</title></head>
  <body>
    <h1>Users (<%= users.length %>)</h1>
    <ul>
      <% users.forEach(user => { %>
        <li><%= user %></li>
      <% }) %>
    </ul>
  </body>
</html>
